version: '3.8'

services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_DB: restaurant
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: adminpasswd
    ports:
      - 5432:5432
    volumes:
      - pg:/var/lib/postgresql
volumes:
  pg: