version: '3.8'

services:
  db-test:
    image: postgres
    restart: always
    environment:
      POSTGRES_DB: test
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
    ports:
      - 5444:5432
    volumes:
      - pg-test:/var/lib/postgresql/data
volumes:
  pg-test: